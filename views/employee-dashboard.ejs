<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Employee Dashboard</title>
    <style>
        /* General Styling */
        body {
            font-family: Arial, sans-serif;
            background-color: #121212;
            color: #e0e0e0;
            text-align: center;
            margin: 0;
            padding: 0;
        }

        header {
            background-color: #1c1c1c;
            padding: 20px;
            font-size: 2rem;
            color: #f58220;
            text-align: center;
        }

        nav {
            display: flex;
            justify-content: center;
            background-color: #1c1c1c;
            padding: 10px 0;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.3);
        }

        nav a {
            color: #e0e0e0;
            text-decoration: none;
            font-size: 1.2rem;
            font-weight: bold;
            margin: 0 20px;
            transition: color 0.3s ease;
        }

        nav a:hover {
            color: #f58220;
        }

        .container {
            margin: 30px auto;
            max-width: 1200px;
            padding: 20px;
        }

        .section {
            background-color: #1c1c1c;
            margin: 20px 0;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
        }

        .section h2 {
            font-size: 1.8rem;
            color: #f58220;
            margin-bottom: 15px;
        }

        .section p {
            font-size: 1.2rem;
            margin-bottom: 10px;
        }

        .section button {
            padding: 10px 20px;
            background-color: #f58220;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.3s ease;
        }

        .section button:hover {
            background-color: #d94e1f;
            transform: scale(1.05);
        }

        footer {
            margin-top: 30px;
            padding: 15px 0;
            background-color: #1c1c1c;
            color: #e0e0e0;
            font-size: 0.9rem;
            text-transform: uppercase;
        }
    </style>
</head>
<body>
    <!-- Header -->
    <header>Employee Dashboard</header>

    <!-- Navigation -->
    <nav>
        <a href="/employee/pos">POS System</a>
        <a href="/employee/sales">Sales History</a>
        <a href="">More Coming Soon</a>
    </nav>

    <!-- Main Container -->
    <div class="container">
        <!-- POS System Section -->
        <div class="section">
            <h2>Point of Sale (POS) System</h2>
            <p>Quickly add items to the cart and complete transactions.</p>
            <button onclick="window.location.href='/employee/pos'">Go to POS</button>
        </div>

        <!-- Sales History Section -->
        <div class="section">
            <h2>Sales History</h2>
            <p>View all your completed transactions and sales details.</p>
            <button onclick="window.location.href='/employee/sales'">View Sales</button>
        </div>

        </div>
    </div>

    <!-- Footer -->
    <footer>
        &copy; 2025 24/7 Supermarket. All Rights Reserved.
    </footer>
</body>
</html>
